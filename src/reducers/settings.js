/**
 * TODO:
 * Implement settings
 */

const initialState = {}

export default (state = initialState, action) => {
  switch (action.type) {
    //case SETTINGS_RESET: initialState;
    default: return state;
  }
};