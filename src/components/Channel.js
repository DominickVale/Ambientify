import React from 'react'
import { Text } from 'react-native'

const Channel = (props) => {
  return (
    <>
      <Text>
        Channel {props.id}
      </Text>
    </>
  )
}

export default Channel
